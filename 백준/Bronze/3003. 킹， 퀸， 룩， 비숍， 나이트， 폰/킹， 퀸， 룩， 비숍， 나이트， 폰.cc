#include <bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    int num = 0;
    cin >> num;
    cout << 1 - num << " ";
    cin >> num;
    cout << 1 - num << " ";
    cin >> num;
    cout << 2 - num << " ";
    cin >> num;
    cout << 2 - num << " ";
    cin >> num;
    cout << 2 - num << " ";
    cin >> num;
    cout << 8 - num;
}